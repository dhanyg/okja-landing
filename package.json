{
  "name": "okja-landing",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "SET NODE_ENV=development && npx tailwindcss-cli@latest build ./src/tailwind.css -o ./css/style.css",
    "serve": "SET NODE_ENV=production && npx tailwindcss-cli@latest build ./src/tailwind.css -o ./css/style.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "jump.js": "^1.0.2",
    "tailwindcss": "^2.0.2"
  }
}